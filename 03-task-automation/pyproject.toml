[project]
name = "avahi-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

# Add real deps here as the pipeline grows, e.g.:
# dependencies = [
#   "langchain>=0.2",
#   "boto3>=1.34",
#   "openai>=1.0",
# ]